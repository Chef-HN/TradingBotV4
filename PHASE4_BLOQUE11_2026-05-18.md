# Fase 4 - Bloque 11 (CI shadow seed + canary extendido postgres_tail)

Fecha: 2026-05-18  
Repo: `C:\Users\Abraham\source\repos\TradingBotV4`

## Objetivo del bloque
1. Corregir fallo de GitHub Actions en `phase4-shadow-gate.yml`.
2. Validar corrida extendida en paralelo V3->BD->V4 con `postgres_tail` sin tocar procesos/codigo V3.

## Problema detectado
En CI, el job `Shadow Diff Baseline Gate` fallaba con:

`RuntimeError: No active strategy row found in tenant_pair_strategies for shadow replay.`

Eso ocurria en entornos limpios donde solo se aplicaban migraciones y no habia fila activa para `SOL-USD`.

## Implementado
1. Seed idempotente para shadow gate:
   - Nuevo script: `scripts/phase4_seed_shadow_strategy.py`
   - Garantiza fila activa en `tenant_pair_strategies` para:
     - tenant `00000000-0000-0000-0000-000000000001`
     - exchange `bybit`
     - product `SOL-USD`
   - Inserta historia inicial en `tenant_pair_strategy_history` solo si falta.
2. Workflow CI actualizado:
   - `.github/workflows/phase4-shadow-gate.yml`
   - Paso nuevo antes del replay:
     - `Seed shadow strategy row (tenant pair, SOL-USD)`
3. Hardening de canary paralelo:
   - `scripts/phase4_canary_smoke.ps1`
   - Para `postgres_tail`, si no se pasan overrides explicitos:
     - `MarketDataGapWarnMs => 7000`
     - `HeartbeatLagWarnMs => 7000`
   - Evita falsos negativos por cadencia externa de ticks (fuente V3).
4. Documentacion:
   - `worker-rust/README.md` actualizado con ajuste automatico de umbrales en `postgres_tail`.

## Validaciones ejecutadas
1. Shadow gate local (strict+intent):
```powershell
.\.venv\Scripts\python.exe scripts/phase4_shadow_diff_replay.py --mode both --replay-path replay/fixtures/solusd_shadow_baseline_v1_20260508.jsonl --python-profile rust_projection --db-dsn "postgresql://tradingbot:tradingbot@127.0.0.1:5443/tradingbotv4_staging" --redis-url "redis://127.0.0.1:6390/15" --enforce-gates --gate-scope both
```
Resultado:
- PASS
- strict ratio: `1.0000`
- intent ratio: `1.0000`

2. Canary extendido (15 min) `postgres_tail` con umbral antiguo (5s):
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\phase4_canary_smoke.ps1 -MarketDataProvider postgres_tail -MarketDataDbDsn postgresql://tradingbot:tradingbot@localhost:5433/tradingbotv3 -DurationSeconds 900
```
Resultado:
- FAIL por alertas:
  - `market_data_gap_detected=11`
  - `heartbeat_lag_detected=7`
- Sin fallos criticos de ejecucion/redis/market provider.
- Rango observado:
  - `gap_ms`: `5150..5980`
  - `heartbeat_gap_ms`: `5667..6498`

3. Canary control con 7s:
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\phase4_canary_smoke.ps1 -MarketDataProvider postgres_tail -MarketDataDbDsn postgresql://tradingbot:tradingbot@localhost:5433/tradingbotv3 -DurationSeconds 300 -MarketDataGapWarnMs 7000 -HeartbeatLagWarnMs 7000
```
Resultado:
- PASS
- `worker_boot=1`, `kernel_bootstrap_grid=1`, `order_submitted=6`
- Sin alertas ni fallos criticos.

4. Canary corto con defaults nuevos (auto-ajuste):
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\phase4_canary_smoke.ps1 -MarketDataProvider postgres_tail -MarketDataDbDsn postgresql://tradingbot:tradingbot@localhost:5433/tradingbotv3 -DurationSeconds 180
```
Resultado:
- PASS
- Se aplico ajuste automatico a `7000ms`.

5. Canary extendido post-ajuste (10 min, defaults nuevos):
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\phase4_canary_smoke.ps1 -MarketDataProvider postgres_tail -MarketDataDbDsn postgresql://tradingbot:tradingbot@localhost:5433/tradingbotv3 -DurationSeconds 600
```
Resultado:
- PASS
- Eventos principales:
  - `worker_boot=1`
  - `kernel_bootstrap_grid=1`
  - `kernel_rebalance_grid=1`
  - `order_submitted=12`
  - `order_canceled=6`
- Sin fallos criticos ni alertas por encima de umbral.

## Evidencia
- `logs/phase4_canary/canary_20260517_233709.out.log` (extendido 15 min, fail por threshold)
- `logs/phase4_canary/canary_20260517_235256.out.log` (control 300s, pass)
- `logs/phase4_canary/canary_20260518_003005.out.log` (defaults nuevos, pass)
- `logs/phase4_canary/canary_20260518_172944.out.log` (extendido 10 min, pass)

## Conclusion
- El paralelo V3+V4 por BD (`postgres_tail`) queda operativo en paper mode.
- El fallo observado fue de calibracion de umbral (no de estabilidad critica).
- Se mantiene el guardrail: V4 no toca ni codigo ni procesos de V3.
